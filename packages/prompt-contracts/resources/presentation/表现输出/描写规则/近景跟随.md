# 近景跟随

> 职责：切镜、密度、铺开顺序。不管用语和题材腔。

## 优先级

切镜 / 锁景 ＞ 密度（铺开或收束）＞ 铺开顺序。顺序只在密度=铺开、且当前距离够宽时生效。

## 切镜策略

锁在当前行动主体能看见、听见、碰到或误读的范围。切口=动作完成或话轮交接。禁止无故切到场外全知或大远景。

## 密度策略

默认中密：手、呼吸、器物、眼前一米。只有首次见面或对峙才允许短暂升密。

## 铺开配方（仅密度=铺开时）

默认截断到最后一槽：一件器物、一张脸、一处伤。不要从头到脚走完。

## 禁止

禁止航拍、禁止场外解说、禁止为交代地图而拉远。
