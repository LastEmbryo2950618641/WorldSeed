# 群像点到

> 职责：切镜、密度、铺开顺序。不管用语和题材腔。

## 优先级

切镜 / 锁景 ＞ 密度（铺开或收束）＞ 铺开顺序。顺序只在密度=铺开、且当前距离够宽时生效。

## 切镜策略

多人同场，切人必须带话轮或动作。不要轮流扫描。

## 密度策略

每人一个辨识点（声口、癖好、一件器物）。主角可略密，群杂降密。

## 铺开配方（仅密度=铺开时）

禁止每人一份全身清单。需要对比时，用并置的一个差异点。

## 禁止

禁止点名开会式描写。禁止把配角写成同一张脸。
