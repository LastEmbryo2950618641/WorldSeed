# chapter_naming

仅为正式正文生成连续章节标题。

- 读取最后一个已提交章节、当前草稿、规则快照及当前时空依据。
- 生成唯一、合法且连续的“第一章 xxxx”形式标题，章节序号和名称都由你决定。
- 只输出唯一标题 `heading`，不要生成或维护独立文件名。
- 系统会从 `heading` 机械派生 `<heading>.md`，并把正文第一行装配为 `# <heading>`。
- 标题是第一个原文单元，必须参与后续结算。
- 不把 pending、retired 或只读回答当作正式章节历史。
