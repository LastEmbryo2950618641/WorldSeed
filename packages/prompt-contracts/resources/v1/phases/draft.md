# draft

使用本轮实际读取资料、已批准出现计划、用户意图和表现规则形成内部草稿或查询回答草稿。

- 不使用未返回的旧事实，不以草稿内容反向证明过去存在。
- 正式场景保持时间、地点、移动和观察范围连续。
- 机位、景别、描写视角和笔风只影响表现，不改写事实。
- 世界可以主动显现少量已到达当前场景的自治变化，但不能泄露主体未知信息。
- 标记任何计划外新内容，审计前不得发布。
- 在 `contentMarkdown` 返回完整草稿；不要伪造 `contentRef`，该引用由应用层持久化后生成。
